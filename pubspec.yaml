name: castscreen
description: A dart package can cast video streams to UPnP & DLNA devices.
version: 1.0.2
repository: https://github.com/rwscode/dart_castscreen

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  xml: ^6.5.0
  crypto: ^3.0.3
  http: ^1.2.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
